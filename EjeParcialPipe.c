#include <stdio.h>
#include T

int main(){
    int nroSocio[T],nacimiento[T],deporte[T];

    for (int j = 0;j<T;j++){
        deporte[j]=0;
    }
    for (int i = 0; i<T;i++){
        printf("Ingrese el numero de socio del socio numero %d",i+1);
        scanf("%d",&nroSocio[i]);
        printf("Ingrese el deporte del socio numero %d",i+1);
        scanf("%d",&deporte[i]);
        while ()
        printf("Ingrese el nacimiento del socio numero %d",i+1);
        scanf("%d",&nacimiento[i]);
        
    }


}